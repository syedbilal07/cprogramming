#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv) {
    int sum = 17, count = 5;
    double mean;
    mean = (double) sum/count;
    printf("Mean Is : %f\n ", mean );
    return (EXIT_SUCCESS);
}

