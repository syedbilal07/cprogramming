#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv) {
    int *ptr = NULL;
    printf("Value Of ptr Variable Is : %p \n", ptr); /*%p to print value in hexadecimal & %x to print the int value*/
    return (EXIT_SUCCESS);
}

